# Golden tests compare against PNGs that are not in git (see .gitignore), so
# CI runs `flutter test --exclude-tags golden`. Locally they run as usual,
# after a first `flutter test --update-goldens`.
tags:
  golden:
